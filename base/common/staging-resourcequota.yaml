apiVersion: v1
kind: ResourceQuota
metadata:
  name: staging-resourcequota
  namespace: staging
spec:
  hard:
    pods: "10"
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi